{
  "name": "@chemicheck119/clawops-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --dir test",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@teamlearners/clawops": "0.45.0",
    "openai": "7.20.0",
    "pino": "10.3.1",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
